apiVersion: v1
kind: Service
metadata:
  name: {{ include "capstone-api.fullname" . }}
  labels:
    {{- include "capstone-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "capstone-api.selectorLabels" . | nindent 4 }}
